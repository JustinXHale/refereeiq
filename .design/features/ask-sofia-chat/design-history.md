# Ask Sofia chat — design history

## 2026-05-14

### [Update] User message timestamp contrast
- Message time under user bubbles uses `onPrimary` (dark ink on the yellow bubble) instead of `onSurfaceVariant`, which was unreadable on the primary-colored fill.
