# Design history format

Use entries like:

```markdown
## YYYY-MM-DD

### [Update] Brief title
- One or two sentences on user-visible impact.
```

See [.design/feature-mapping.md](feature-mapping.md) for which file owns each feature area.
